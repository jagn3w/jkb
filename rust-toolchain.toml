# Pin the toolchain so every machine/CI builds jkb with the same compiler.
# rustup reads this automatically and installs the channel + components on demand.
[toolchain]
channel = "1.96.1"
components = ["rustfmt", "clippy"]
profile = "minimal"
