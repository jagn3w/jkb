{
  "name": "@jkb/ui",
  "version": "0.1.0",
  "private": true,
  "description": "jkb UI workspace: a portable core + host adapters (VS Code first, web later).",
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm --filter @jkb/core run build && pnpm -r run typecheck",
    "test": "pnpm -r run test"
  }
}
